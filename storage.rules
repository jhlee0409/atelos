rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 시나리오 이미지
    match /scenarios/{scenarioId}/{allPaths=**} {
      // 모든 사용자가 읽기 가능
      allow read: if true;

      // 쓰기는 서버 사이드에서만 (API 통해서)
      allow write: if true;
    }
  }
}
